[uwsgi]

# we want to host an app in the app module and in the app callable
module = app
callable = app

static-map = /static=/app/static


# how long uwsgi will wait for other containers to answer before killing the connection
socket-timeout = 10000
