name: com.bonsyes.data.training-tensors.0.1

title: Training and validation tensors

description: |
  This format is used to store training and validation tensors. The two
  sets of data are stored as com.bonsyes.data.data-tensors.

extends: com.bonsyes.data.composite-directory.0.1

on_disk_representation: |
  The validation data is stored in a file name 'validation', the training
  data is stored in a file name 'learning'.