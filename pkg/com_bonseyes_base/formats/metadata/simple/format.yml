name: com.bonsyes.metadata.simple.0.1

title: Simple JSON based metadata

description: |
  This format is a generic metadata container. The metadata is stored
  as a JSON object.

extends: com.bonsyes.format.blob.0.1

on_disk_representation: |
  The on-disk representation of this format is a single file containing
  a JSON serialized object.

http_api:
  - endpoint: /
    description: |
      Returns the metadata as JSON