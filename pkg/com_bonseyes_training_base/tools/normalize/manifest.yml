description: "Normalizes a dataset_processing using the given normalization parameters"

execution:
  type: docker-image
  image: com.bonseyes.training.base.normalize

output_type: com.bonseyes.data-tensor

actions:
  create:
    tensor:
      label: "Tensor"
      type: resource
    parameters:
      lable: "Normalization parameters"
      type: resource