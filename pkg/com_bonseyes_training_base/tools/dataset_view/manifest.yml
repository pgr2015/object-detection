description: Create a subset of a dataset_processing

execution:
  type: docker-image
  image: com.bonseyes.training.base.dataset_processing-view

output_type: com.bonseyes.dataset_processing

actions:
  create:
    datasets:
      label: "Original datasets"
      type: json
    condition:
      label: "Jinja2 condition to filter samples"
      type: string