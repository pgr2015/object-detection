description: "Computes the accuracy of the model"

output:
  data_format: com.bonseyes.data.blob.0.1

image:
  image_type: dockerfile
  name: com.bonseyes.reference.objectdetection.training-caffe
  dockerfile:
    - ./tools/objectdetection_benchmark/Dockerfile-base
    - ./pkg/com_bonseyes_base/images/base/Dockerfile
    - ./tools/objectdetection_benchmark/Dockerfile

actions:
  create:
    parameters:
      #caffemodel:
      #  label: "Caffe model"
      #  type: string
      # tensor:
      #   label: "Tensor with test data"
      #   type: resource
      #   data_format: com.bonseyes.data.data-tensors.0.1
      
      model:
         label: "Model"
         type: resource
         data_format: com.bonseyes.data.blob.0.1
      training_set:
         label: "Training Set"
         type: resource
         #data_format: com.bonseyes.data.data-tensors.0.1
      epochs:
        label: "Number of epochs for benchmarking"
        type: string
        optional: true
      batch:
        label: "Batch size"
        type: string
        optional: true  
